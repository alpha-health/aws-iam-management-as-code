#########################################################################
## This file is automatically generated, do NOT modify manually.       ##
## Check the "Viewed" button in the Github PR to hide me.              ##
#########################################################################

AWSTemplateFormatVersion: 2010-09-09
Description: Personal IAM Profile for DeveloperCc

Resources:
  PersonalPolicyS3DeveloperCc:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Personal Policy S3 for DeveloperCc
      ManagedPolicyName: PersonalPolicyS3DeveloperCc
      Path: /
      PolicyDocument:
        Version: 2012-10-17
        Statement: 
          # S3AllowReadOnBucketOperationsb
          - Effect: "Allow"
            Action:
              - "s3:GetObject"
              - "s3:GetObjectAcl"
              - "s3:GetObjectVersion"
              - "s3:ListBucket"
              - "s3:ListObjectVersions"
              - "s3:GetBucketLocation"
            Resource:
              - "arn:aws:s3:::operationsb"
              - "arn:aws:s3:::operationsb/*"
          # S3AllowWriteOnBucketOperationsb
          - Effect: "Allow"
            Action:
              - "s3:PutObject"
              - "s3:PutObjectAcl"
            Resource:
              - "arn:aws:s3:::operationsb/*"
  PersonalPolicySecretManagerDeveloperCc:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Personal Policy SecretManager for DeveloperCc
      ManagedPolicyName: PersonalPolicySecretManagerDeveloperCc
      Path: /
      PolicyDocument:
        Version: 2012-10-17
        Statement: 
          # SecretManagerBoxReadOnly
          - Effect: "Allow"
            Action:
              - "secretsmanager:GetSecretValue"
              - "secretsmanager:DescribeSecret"
              - "secretsmanager:ListSecrets"
              - "secretsmanager:TagResource"
            Resource:
              - "arn:aws:secretsmanager:*:*:secret:Box/*"

  PersonalRoleDeveloperCc:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
        - Effect: Allow
          Principal:
            Service:
            - ec2.amazonaws.com
          Action:
          - sts:AssumeRole
        - Effect: Allow
          Principal:
            AWS:
            - arn:aws:iam::123456789876:user/ServiceUserProdKrun
          Action:
          - sts:AssumeRole
      Path: "/"
      ManagedPolicyArns:
        - !Ref PersonalPolicyS3DeveloperCc
        - !Ref PersonalPolicySecretManagerDeveloperCc
      RoleName: PersonalRoleDeveloperCc

  PersonalInstanceProfileDeveloperCc:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: PersonalInstanceProfileDeveloperCc
      Path: "/"
      Roles:
      - !Ref PersonalRoleDeveloperCc