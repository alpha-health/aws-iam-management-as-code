#########################################################################
## This file is automatically generated, do NOT modify manually.       ##
## Check the "Viewed" button in the Github PR to hide me.              ##
#########################################################################

AWSTemplateFormatVersion: 2010-09-09
Description: Personal IAM Profile for DeveloperDd

Resources:
  PersonalPolicyS3DeveloperDd:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Personal Policy S3 for DeveloperDd
      ManagedPolicyName: PersonalPolicyS3DeveloperDd
      Path: /
      PolicyDocument:
        Version: 2012-10-17
        Statement: 
          # S3AllowReadOnBucketOperationsb
          - Effect: "Allow"
            Action:
              - "s3:GetObject"
              - "s3:GetObjectAcl"
              - "s3:GetObjectVersion"
              - "s3:ListBucket"
              - "s3:ListObjectVersions"
              - "s3:GetBucketLocation"
            Resource:
              - "arn:aws:s3:::operationsb"
              - "arn:aws:s3:::operationsb/*"
          # S3AllowWriteOnBucketOperationsb
          - Effect: "Allow"
            Action:
              - "s3:PutObject"
              - "s3:PutObjectAcl"
            Resource:
              - "arn:aws:s3:::operationsb/*"
  PersonalPolicySecretManagerDeveloperDd:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Personal Policy SecretManager for DeveloperDd
      ManagedPolicyName: PersonalPolicySecretManagerDeveloperDd
      Path: /
      PolicyDocument:
        Version: 2012-10-17
        Statement: 
          # SecretManagerBoxReadOnly
          - Effect: "Allow"
            Action:
              - "secretsmanager:GetSecretValue"
              - "secretsmanager:DescribeSecret"
              - "secretsmanager:ListSecrets"
              - "secretsmanager:TagResource"
            Resource:
              - "arn:aws:secretsmanager:*:*:secret:Box/*"

  PersonalRoleDeveloperDd:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
        - Effect: Allow
          Principal:
            Service:
            - ec2.amazonaws.com
          Action:
          - sts:AssumeRole
        - Effect: Allow
          Principal:
            AWS:
            - arn:aws:iam::123456789876:user/ServiceUserProdKrun
          Action:
          - sts:AssumeRole
      Path: "/"
      ManagedPolicyArns:
        - !Ref PersonalPolicyS3DeveloperDd
        - !Ref PersonalPolicySecretManagerDeveloperDd
      RoleName: PersonalRoleDeveloperDd

  PersonalInstanceProfileDeveloperDd:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: PersonalInstanceProfileDeveloperDd
      Path: "/"
      Roles:
      - !Ref PersonalRoleDeveloperDd